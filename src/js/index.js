const { Router, Route, Link, IndexRoute, browserHistory, hashHistory } = ReactRouter;const LessonDetail=require('../common/lessonDetail');const LessonSign=require('../common/lessonSign');const App=React.createClass({    render (){        return (<div>            {this.props.children}        </div>)    },    componentDidMount () {            }});const Main=React.createClass({    render (){        return (<div>            您访问的页面不存在!        </div>)    }});ReactDOM.render((  <Router history={browserHistory}>    <Route path="/" component={App}>        <IndexRoute component={Main} />        <Route path="lessonDetail" component={LessonDetail} />        <Route path="lessonSign" component={LessonSign} />    </Route>  </Router>), document.getElementById('J_Container'));