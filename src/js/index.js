import React from 'react'import { render } from 'react-dom'import { Router, Route, Link, IndexRoute, browserHistory, hashHistory } from 'react-router'// const { Router, Route, Link, IndexRoute, browserHistory, hashHistory } = ReactRouter;const LessonDetail=require('../common/lessonDetail.js');const LessonSign=require('../common/lessonSign.js');const Index =require('../common/index.js');require('../css/common.scss');const App=React.createClass({    render (){        return (<div>            { this.props.children }        </div>)    },    componentDidMount () {            }});const Main=React.createClass({    render (){        return (<div>            您访问的页面不存在!        </div>)    }});render((  <Router history={browserHistory}>    <Route path="/" component={App}>        <IndexRoute component={Index} />        <Route path="lessonDetail" component={LessonDetail} />        <Route path="lessonSign" component={LessonSign} />    </Route>  </Router>), document.getElementById('J_Container'));